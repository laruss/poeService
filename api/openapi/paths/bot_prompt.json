{
  "post": {
    "tags": [
      "Prompt"
    ],
    "summary": "Set bot main prompt",
    "description": "Set bot main prompt (in Poe and local)",
    "produces": [
      "application/json"
    ],
    "parameters": [
      {
        "$ref": "../definitions/bot_name_path.json"
      },
      {
        "name": "body",
        "in": "body",
        "description": "Bot main prompt",
        "required": true,
        "schema": {
          "type": "object",
          "properties": {
            "prompt": {
              "type": "string"
            }
          },
          "required": [
            "prompt"
          ]
        }
      }
    ],
    "responses": {
      "204": {
        "description": "successful operation"
      },
      "403": {
        "description": "Poe API key is not set"
      },
      "404": {
        "description": "Bot not found"
      },
      "500": {
        "description": "Internal server error"
      }
    }
  }
}